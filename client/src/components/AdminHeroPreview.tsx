import type { HeroSettings } from "@/lib/adminCommerce";
import { ArrowRight, ImageOff } from "lucide-react";

export function AdminHeroPreview({ hero }: { hero: HeroSettings }) {
  return <section className="mt-6 overflow-hidden rounded-[2rem] bg-[linear-gradient(120deg,#FFFFFF_0%,#F3F8FF_48%,#EAFBFF_100%)] p-5 shadow-[0_16px_36px_rgba(20,68,145,.1)] sm:p-7"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="eyebrow">Aperçu avant publication</p><h3 className="mt-1 font-display text-xl font-bold text-[#081A3C]">Bannière d’accueil</h3></div><span className="rounded-full bg-blue-100 px-3 py-1.5 text-xs font-extrabold text-blue-700">FR / EN</span></div><div className="mt-5 grid gap-5 lg:grid-cols-2"><HeroCard eyebrow={hero.eyebrowFr} title={hero.titleFr} body={hero.bodyFr} cta={hero.primaryCtaFr} image={hero.imageUrl} label="Français" /><HeroCard eyebrow={hero.eyebrowEn} title={hero.titleEn} body={hero.bodyEn} cta={hero.primaryCtaEn} image={hero.imageUrl} label="English" /></div></section>;
}
function HeroCard({ eyebrow, title, body, cta, image, label }: { eyebrow: string; title: string; body: string; cta: string; image: string; label: string }) { return <article className="overflow-hidden rounded-2xl border border-blue-100 bg-white"><div className="p-5"><p className="text-[0.65rem] font-extrabold uppercase tracking-[.14em] text-blue-700">{label} · {eyebrow}</p><h4 className="mt-3 font-display text-2xl font-bold leading-tight tracking-[-.04em] text-[#081A3C]">{title}</h4><p className="mt-3 text-sm leading-6 text-slate-600">{body}</p><span className="mt-5 inline-flex items-center gap-2 rounded-xl bg-blue-700 px-4 py-2.5 text-sm font-extrabold text-white">{cta}<ArrowRight className="h-4 w-4" /></span></div>{image ? <img src={image} alt="Aperçu du visuel de bannière" className="aspect-[16/6] w-full object-cover" /> : <div className="flex aspect-[16/5] items-center justify-center gap-2 bg-blue-50 text-xs font-bold text-blue-600"><ImageOff className="h-4 w-4" />Visuel actuel de la sélection produits</div>}</article>; }
