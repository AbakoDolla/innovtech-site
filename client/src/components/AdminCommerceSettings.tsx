import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import type { CommercialSettings, HeroSettings } from "@/lib/adminCommerce";
import type { SupabaseMediaAsset } from "@/lib/supabaseMedia";
import { ImagePlus, Loader2, Save, Settings2 } from "lucide-react";

type Props = {
  commercial: CommercialSettings;
  hero: HeroSettings;
  assets: SupabaseMediaAsset[];
  onCommercialChange: (value: CommercialSettings) => void;
  onHeroChange: (value: HeroSettings) => void;
  pending: boolean;
  onSave: () => void;
};

export function CommerceSettingsPanel({ commercial, hero, assets, onCommercialChange, onHeroChange, pending, onSave }: Props) {
  const setCommercial = <K extends keyof CommercialSettings>(key: K, value: CommercialSettings[K]) => onCommercialChange({ ...commercial, [key]: value });
  const setHero = <K extends keyof HeroSettings>(key: K, value: HeroSettings[K]) => onHeroChange({ ...hero, [key]: value });
  const images = assets.filter((asset) => asset.media_type === "image");
  return <section className="mt-8"><div><p className="eyebrow">Diffusion publique</p><h2 className="mt-2 font-display text-2xl font-bold tracking-[-.04em] text-[#081A3C]">Réglages commerciaux et accueil</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">Ces informations sont enregistrées dans Supabase puis utilisées par le site public. Les textes sont distincts en français et en anglais.</p></div><div className="mt-5 grid gap-5 xl:grid-cols-2"><article className="rounded-3xl border border-blue-100 bg-white p-5 shadow-sm sm:p-7"><div className="flex items-center gap-3"><Settings2 className="h-5 w-5 text-blue-700" /><h3 className="font-display text-xl font-bold text-[#081A3C]">Commerce et WhatsApp</h3></div><div className="mt-5 grid gap-4"><Field label="Numéro WhatsApp (chiffres internationaux)" value={commercial.whatsappNumber} onChange={(value) => setCommercial("whatsappNumber", value.replace(/\D/g, ""))} /><label className="grid gap-2 text-sm font-bold text-slate-700">Message de commande FR<small className="font-medium text-slate-500">Utilisez {"{produit}"} pour insérer le nom du produit.</small><Textarea rows={3} value={commercial.orderMessageFr} onChange={(event) => setCommercial("orderMessageFr", event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-700">Message de commande EN<small className="font-medium text-slate-500">Utilisez {"{product}"} pour insérer le nom du produit.</small><Textarea rows={3} value={commercial.orderMessageEn} onChange={(event) => setCommercial("orderMessageEn", event.target.value)} /></label><Field label="Délai de réponse FR" value={commercial.responseTimeFr} onChange={(value) => setCommercial("responseTimeFr", value)} /><Field label="Délai de réponse EN" value={commercial.responseTimeEn} onChange={(value) => setCommercial("responseTimeEn", value)} /><label className="grid gap-2 text-sm font-bold text-slate-700">Livraison FR<Textarea rows={2} value={commercial.deliveryInfoFr} onChange={(event) => setCommercial("deliveryInfoFr", event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-700">Livraison EN<Textarea rows={2} value={commercial.deliveryInfoEn} onChange={(event) => setCommercial("deliveryInfoEn", event.target.value)} /></label></div></article><article className="rounded-3xl border border-blue-100 bg-white p-5 shadow-sm sm:p-7"><div className="flex items-center gap-3"><ImagePlus className="h-5 w-5 text-blue-700" /><h3 className="font-display text-xl font-bold text-[#081A3C]">Bannière d’accueil</h3></div><div className="mt-5 grid gap-4"><Field label="Surtitre FR" value={hero.eyebrowFr} onChange={(value) => setHero("eyebrowFr", value)} /><Field label="Surtitre EN" value={hero.eyebrowEn} onChange={(value) => setHero("eyebrowEn", value)} /><label className="grid gap-2 text-sm font-bold text-slate-700">Titre FR<Textarea rows={2} value={hero.titleFr} onChange={(event) => setHero("titleFr", event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-700">Titre EN<Textarea rows={2} value={hero.titleEn} onChange={(event) => setHero("titleEn", event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-700">Texte FR<Textarea rows={3} value={hero.bodyFr} onChange={(event) => setHero("bodyFr", event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-700">Texte EN<Textarea rows={3} value={hero.bodyEn} onChange={(event) => setHero("bodyEn", event.target.value)} /></label><Field label="Bouton FR" value={hero.primaryCtaFr} onChange={(value) => setHero("primaryCtaFr", value)} /><Field label="Bouton EN" value={hero.primaryCtaEn} onChange={(value) => setHero("primaryCtaEn", value)} /><Field label="Lien du bouton" value={hero.primaryHref} onChange={(value) => setHero("primaryHref", value)} placeholder="/boutique" /><label className="grid gap-2 text-sm font-bold text-slate-700">Image principale depuis la bibliothèque<select className="h-11 rounded-xl border border-slate-200 bg-white px-3" value={hero.imageUrl} onChange={(event) => setHero("imageUrl", event.target.value)}><option value="">Sélection actuelle de produits</option>{images.map((asset) => <option key={asset.id} value={asset.public_url}>{asset.title}</option>)}</select><small className="font-medium text-slate-500">Ajoutez d’abord une photo dans l’éditeur produit, puis sélectionnez-la ici.</small></label>{hero.imageUrl && <img src={hero.imageUrl} alt="Aperçu de la bannière" className="aspect-[16/9] w-full rounded-2xl border border-blue-100 object-cover" />}</div></article></div><div className="mt-6 flex justify-end"><Button size="lg" disabled={pending} onClick={onSave}>{pending ? <Loader2 className="mr-2 h-4 w-4 animate-spin" /> : <Save className="mr-2 h-4 w-4" />}Enregistrer les réglages</Button></div></section>;
}

function Field({ label, value, onChange, placeholder = "" }: { label: string; value: string; onChange: (value: string) => void; placeholder?: string }) { return <label className="grid gap-2 text-sm font-bold text-slate-700">{label}<Input value={value} placeholder={placeholder} onChange={(event) => onChange(event.target.value)} /></label>; }
